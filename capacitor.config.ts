import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppPrevencionDesastres',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
